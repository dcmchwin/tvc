from setuptools import setup

version = '0.0.0'

setup(name='tvc',
      version=version,
      packages=[],
      test_required=[],
      entry_points={
          'console_scripts': [
          ],
      }
      )
